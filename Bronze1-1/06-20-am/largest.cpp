#include <bits/stdc++.h>
using namespace std;

int main() {
	int N; cin >> N; //# of integers
	int A[N];
	for(int j=0; j<N; j++) {
		cin >> A[j];
	}
	
	//count the number of integers that are larger than any element
	//  to the left
	int count = 0;
	
	
	
	
	cout << count << '\n';
}

